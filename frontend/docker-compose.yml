version: '3.9'
services:
  mongo:
    image: mongo:7
    container_name: ai-interview-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: ai_interview_prep
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
